using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using GOAP.Framework;
public class TestFun : MonoBehaviour {

    public PEMethod method = null;
    private void Update()
    {
        if(Input.GetKeyDown(KeyCode.T))
        {
            if(method != null)
            {
                
            }
        }
    }
}
